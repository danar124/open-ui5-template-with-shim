<mvc:View
	controllerName="sap.ui.demo.basicTemplate.controller.Detail"
	xmlns="sap.m"
	xmlns:mvc="sap.ui.core.mvc"
	xmlns:f="sap.ui.layout.form"
	busyIndicatorDelay="0"
>
	<Page
		title="{i18n>DetailOf}"
		showNavButton="true"
		navButtonPress="onNavBack"
		class="sapUiResponsiveContentPadding"
	>
		<content>
			<Panel
				id="employeePanel"
				width="auto"
				class="sapUiResponsiveMargin sapUiNoContentPadding"
			>
				<headerToolbar>
					<Toolbar>
						<Title
							text="{i18n>Belnr} {Belnr}"
							level="H2"
						/>
						<ToolbarSpacer/>
					</Toolbar>
				</headerToolbar>
				<content>
					<f:SimpleForm
						minWidth="1024"
						editable="false"
						layout="ResponsiveGridLayout"
						labelSpanL="3"
						labelSpanM="3"
						emptySpanL="4"
						emptySpanM="4"
						columnsL="1"
					>
						<f:content>
							<Label text="{i18n>Belnr}"/>
							<Text text="{Belnr}"/>
							<Label text="{i18n>Bukrs}"/>
							<Text text="{Bukrs}"/>
							<Label text="{i18n>CitiPayStatus}"/>
							<Text text="{CitiPayStatus}"/>
							<Label text="{i18n>Gjahr}"/>
							<Text text="{Gjahr}"/>
							<Label text="{i18n>SftpStatus}"/>
							<Text text="{SftpStatus}"/>
						</f:content>
					</f:SimpleForm>
				</content>
			</Panel>
		</content>
	</Page>
</mvc:View>